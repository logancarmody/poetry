== Google Poetry

A Web Application which generates random poetry based on one or more Google and Wikipedia searches.

== Requirements

== Getting Started

To get started, clone the repo, <tt>bundle</tt>, <tt>rake db:init && rake db:migrate</tt> and you're good to go. <tt>rails s</tt> to start the server.
